# V课表公测版

这是独立于正式版的通用课表导入试验站。它是纯静态 PWA，打开 `index.html` 即可运行，数据保存在浏览器本地。

## 主要变化

- 不预置个人课程，默认每天固定 10 节课；
- 作息只使用第一节开始时间、每节时长和统一课间，不包含特定教学楼的时间判断；
- 支持 AI 生成 JSON 粘贴导入；
- 支持 Safari 快捷指令运行正方 V9 页面适配器，再粘贴结果导入；
- 公测版使用独立 localStorage 键，不会覆盖正式版。

部署时上传本目录全部文件，包括 `adapters/` 和 `icons/`。
